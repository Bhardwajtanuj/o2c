
  # o2c

  This is a code bundle for o2c. The original project is available at https://www.figma.com/design/0zhyO2H2cM4PAlbS1lIGxK/o2c.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  